# 必备：复制为本文件同目录下的 .env（文件名必须是 .env），再把 IP 改成你电脑的局域网地址。
# Expo 只会读取 app/.env，不会读取 .env.example。
#
# 模拟器：可以用 127.0.0.1
# 真机：必须用 Mac 局域网 IP（与 Metro 里 exp://x.x.x.x 同一网段），并同时修改 web/.env.development 里的 VITE_API_BASE_URL。
#
EXPO_PUBLIC_WEB_BASE_URL=http://127.0.0.1:5173
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
