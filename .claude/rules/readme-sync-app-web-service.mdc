---
description: app/web/service 目录变更时同步更新各自 README 目录树与概要
alwaysApply: true
---

# 多端 README 与目录结构同步

当 **`app/`、`web/`、`service/`** 任一端的**目录结构**发生变化时（新增/删除/移动/重命名目录或关键文件、显著调整分层），在同一提交或连贯任务内 **必须** 更新该端根目录下的 **`README.md`**：

1. **目录树**：与仓库实际结构一致（可用 `tree` 或等价列表；风格与该 README 既有写法对齐）。
2. **内容概要**：树中每个文件的简短说明保持更新（遵守该 README 约定的字数，例如单行 `#` 注释式概要）。
3. **`service/`** 若尚无 `README.md`，在首次需要记录结构时 **创建** 根级 `service/README.md`，并包含简介 + 目录树 + 概要。

## 触发示例

- 在 `app/modules/` 下新增模块、`web/src/` 下新目录、`service/` 下新包或路由文件。
- 重构导致路径大面积变动。

## 不必强行全文重写的场景

- 仅改文件**内部实现**、单行文案，且**路径与文件清单未变**：可不碰 README。

完成结构类改动后，自检：**对应端 `README.md` 中的树是否仍完整、概要是否仍指向正确文件**。
