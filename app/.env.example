# 复制为 app/.env 后按需修改。
# 模拟器：可用下面 localhost。
# 真机（Expo Go）：必须把两处都改成你电脑的局域网 IP（与 Metro 里 exp://x.x.x.x 同一网段），并保证 pnpm dev:web / pnpm dev:service 已启动。
EXPO_PUBLIC_WEB_BASE_URL=http://127.0.0.1:5173
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
