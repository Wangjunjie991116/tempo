---
description: 持久化用毫秒时间戳为真源；UI 由毫秒转换展示；日程卡片时段格式
alwaysApply: true
---

# 时间与存储（毫秒真源）

## 持久化

- 写入用户本地（如 AsyncStorage）或未来服务端数据库的 **日期时间**，一律使用 **Unix 毫秒时间戳（`number`）** 作为 **唯一真源**。
- **不要**把 ISO 字符串或地区性格式字符串当作正式存储形态；若读取旧数据需要兼容，仅在 **读路径** 将字符串解析为毫秒后再进入领域模型。
- 业务逻辑、分区、排序均以 **毫秒数值** 为准。

## 展示层

- 页面与卡片文案：**必须**从毫秒转换为 `Date` 或经统一格式化函数输出后再渲染；避免在 UI 层假定持久化格式为字符串。

## 日程卡片时间段（无星期）

- **不展示**星期缩写（如 Sun、周一）。
- **同一本地自然日**内：`YYYY/M/D HH:mm - HH:mm`（日期仅写一次；月、日不补零；时与分建议两位，如 `09:05`）。
- **跨本地自然日**：`YYYY/M/D HH:mm - YYYY/M/D HH:mm`。

实现参考：`app/modules/schedule/utils/formatScheduleCardTimeRange.ts`。
