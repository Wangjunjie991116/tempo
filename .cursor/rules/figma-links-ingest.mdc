---
description: 用户提供 Figma 链接与说明时，自动写入 docs/figma 下的 Markdown 索引
alwaysApply: true
---

# Figma 链接沉淀（Tempo）

当用户在对话中提供 **`figma.com/design`**（或 Figma 设计稿）**完整链接**及对应用途说明时：

1. **在本次回应中**将链接与大意写入 `docs/figma/`：
   - 日程 RN、通知收件箱、设计令牌等与 `docs/figma/schedule-rn.md` 范围一致时，**更新**该文件（「整体项目入口」「节点直达链接」表格、「节点对照表」）。
   - 其他模块则新建 `docs/figma/<主题简述>.md`，并更新 `docs/figma/README.md` 的「文档一览」表。
2. **优先保留用户给出的完整 URL**（含 `t=`、`node-id`、URL 编码后的文件名片段）；若表格内链接展示过长，「复制用」纯文本块须保留与用户一致的完整链接。
3. **校对**：`node-id` 连字符与粘贴一致；用户用词（如消息 / 通知 / 空状态）写入「说明」列，并与代码模块备注对齐。
4. **默认执行**：除非用户明确说不要写入仓库或仅临时使用，否则完成文档更新；若会话正在整理提交，可一并 `git add` / `commit`。
