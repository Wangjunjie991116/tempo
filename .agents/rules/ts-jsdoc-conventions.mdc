---
description: app/web 下 TypeScript 源码的 TSDoc 注释约定；格式与转换类函数必须附 @example
globs:
  - app/**/*.ts
  - web/**/*.ts
---

# TypeScript 文档注释（TSDoc / JSDoc）

## 适用范围

- **`app/**/*.ts`**、**`web/**/*.ts`**（含根目录配置型 `.ts`，如 `vite.config.ts`）。
- **`*.test.ts`**、`**/__tests__/**`：以断言表达行为即可；仅需对复杂工厂或共用 fixture 写简短说明。

## 书写形式

- 使用块注释 **`/** ... */`**（文档风格），勿用单行 `//` 充当对外 API 文档。
- **导出**的函数、类、主要常量、`interface` / `type`（面向调用方）：须有**首行摘要**（一句话）；参数或返回值非显而易见时补 **`@param`**、**`@returns`**。
- **模块聚合导出**（如 `index.ts`）：可用一行模块说明导出意图。
- **非导出**的内部函数：至少一行摘要说明职责；若参与数据塑形或与导出 API 强相关，可与导出函数同级详细说明。

## 格式转化 / 解析 / 归一化（必须附示例）

以下类别的函数（名称或职责命中即可，如 `format*`、`parse*`、`coerce*`、`to*`、`normalize*`、`serialize*`、`map*`、`from*`、`as*`）：

- 除摘要与 `@param` / `@returns` 外，**必须**包含 **`@example`**。
- `@example` 内使用 **Markdown 围栏代码块**（```` ```ts ```` 或 `tsx`），演示**典型输入 → 输出或用法**，与真实类型一致；避免无意义的占位符。

示例骨架：

```ts
/**
 * 一句话说明转化做什么（例如本地时区下的展示格式）。
 *
 * @param ms Unix 毫秒时间戳
 * @returns 展示用字符串
 *
 * @example
 * ```ts
 * fn(0); // => "1970/1/1 08:00"
 * ```
 */
```

## 与仓库其它约定对齐

- 时间字段的存储与展示仍需遵守 **`.cursor/rules/datetime-ms-source-of-truth.mdc`**；注释里说明参数语义时写明「毫秒」或「ISO 仅兼容读」等，避免与实现矛盾。
